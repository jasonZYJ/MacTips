### 临时一键取消 OS X 开机启动项

需求与解决方法

在系统的`用户与群组`-`登陆项`中，我们可以设置在开机之后，自动启动指定的应用程序，但添加过多的开机启动项，势必会对开机速度造成影响。

如果在某次开机时，你希望**临时**取消那些开机启动项目，以便很快进入系统，这时可以在开机之后，在系统进入`登陆界面`的时候，按住 `Shift(⇧)` 键，同时按下回车。

如果你设置了开机密码，则是在输入密码后，首先按住 `Shift(⇧)` 键，然后按 `Return(↵)` 回车键登陆。

这样，系统在这次启动中，就不会加载用户设置的开机启动项了。